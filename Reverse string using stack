string reverse(string S, int len)
{
    stack<char>st;
    for(int i=0;i<len;i++){
        st.push(S[i]);
    }
    
    string ans="";
    while(!st.empty()){
        ans+=st.top();
        st.pop();
    }
    return ans;
}
